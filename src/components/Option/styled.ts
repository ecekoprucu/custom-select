import styled from 'styled-components'

export const Option = styled.option`
    color: black;
    font-size: 12px;
    width: 95%;
    cursor: pointer;
    text-align: start;
    &:hover {
        background-color: #eeeffb;
        font-weight: 500;
    }
    padding: 5px;
    border-radius: 5px;
`